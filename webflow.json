{
  "cloud": {
    "framework": "custom",
    "nodeVersion": "18",
    "installCommand": "corepack enable && corepack prepare pnpm@8 --activate && pnpm install --frozen-lockfile || pnpm install",
    "buildCommand": "pnpm build",
    "outputDirectory": ".next"
  }
}
